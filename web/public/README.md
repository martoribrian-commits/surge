# Audio assets

Add the following files to this directory before running the app:

- `chaosNoise.mp3` — looping chaos noise bed (30–60 s seamless loop)
- `heartbeat.mp3` — single heartbeat sample (~1 s, seamless loop)

These are loaded by `useSurgeEngine` via the Web Audio API on first user interaction.
