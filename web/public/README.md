# Audio stems

Replace these placeholder files with high-fidelity production assets:

- `chaosNoise.wav` — dense, overwhelming noise bed (seamless loop, 30–60 s)
- `heartbeat.wav` — deep sub-bass pulse sample (seamless loop, ~1 s @ 60 BPM)

Loaded and decoded into memory on mount by `useSurgeEngine.js`.
