[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Surge is a single-page app; route all paths to the SPA entry.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
